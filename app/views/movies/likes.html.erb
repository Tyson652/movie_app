<p id="notice"><%= notice %></p>

<h1>My Likes</h1>

<%# <div class="button">
  <%= link_to 'New Movie', new_movie_path %>
<%# </div> %> 

<table>
  <%# <thead>
    <tr>
      <th>Title</th>
      <th>Genre</th>
      <th>Runtime</th>
      <th colspan="3"></th>
    </tr>
  </thead> %>

  <tbody class="images_div">
    <%# <% @my_likes = Like.all.where(user_id: current_user.id) %> 
    <% @my_likes.each do |movie| %>
      <tr>
        <%# <td><%= movie.title %></td> 
        <%# <td><%= movie.genre %></td> 
        <%# <td><%= movie.runtime %></td> 
        <td class="images"><%= link_to image_tag(movie.thumbnail), movie_reviews_path(movie) %></td>
        <%# <td><%= link_to 'Show', movie_reviews_path(movie) %></td> 
        <%# <td><%= link_to 'Edit', edit_movie_path(movie) %></td> 
        <%# <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>  
      </tr>
    <% end %>
  </tbody>
</table>